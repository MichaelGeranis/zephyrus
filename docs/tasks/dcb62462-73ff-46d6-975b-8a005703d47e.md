# Task Breakdown: as-a-user-of-zephyrus-ui-i-need-to-be-able-to-delete-project

Total tasks: 11

## Task 1: Add cascade delete constraints to foreign key relationships
**Agent:** DB

Update the Entity Framework Core model configuration to enable cascade deletes for the existing foreign key relationships.

## Acceptance Criteria
- [ ] Features.ProjectId → Projects.Id relationship configured with ON DELETE CASCADE
- [ ] Artifacts.FeatureId → Features.Id relationship configured with ON DELETE CASCADE
- [ ] PipelineSteps.ProjectId → Projects.Id relationship configured with ON DELETE CASCADE
- [ ] Create migration named `20250XXX_AddCascadeDeleteConstraints`
- [ ] Migration applies successfully to development database
- [ ] Migration includes proper rollback logic
- [ ] Verify cascade behavior works correctly in test environment

## Task 2: Implement DeleteProjectUseCase
**Agent:** BE

Create a use case class to handle project deletion with proper error handling and validation.

## Acceptance Criteria
- [ ] Create `DeleteProjectUseCase` class in Application layer
- [ ] Verify project exists before attempting deletion
- [ ] Use existing repository pattern to delete project entity
- [ ] Handle EntityNotFoundException when project doesn't exist
- [ ] Handle database constraint violations with appropriate error messages
- [ ] Return appropriate success/failure result
- [ ] Follow existing use case patterns in the codebase
- [ ] Include proper transaction handling

## Task 3: Implement DeleteFeatureUseCase
**Agent:** BE

Create a use case class to handle feature deletion with proper error handling and validation.

## Acceptance Criteria
- [ ] Create `DeleteFeatureUseCase` class in Application layer
- [ ] Verify feature exists before attempting deletion
- [ ] Use existing repository pattern to delete feature entity
- [ ] Handle EntityNotFoundException when feature doesn't exist
- [ ] Handle database constraint violations with appropriate error messages
- [ ] Return appropriate success/failure result
- [ ] Follow existing use case patterns in the codebase
- [ ] Include proper transaction handling

## Task 4: Implement DeleteArtifactUseCase
**Agent:** BE

Create a use case class to handle artifact deletion with proper error handling and validation.

## Acceptance Criteria
- [ ] Create `DeleteArtifactUseCase` class in Application layer
- [ ] Verify artifact exists before attempting deletion
- [ ] Use existing repository pattern to delete artifact entity
- [ ] Handle EntityNotFoundException when artifact doesn't exist
- [ ] Handle database constraint violations with appropriate error messages
- [ ] Return appropriate success/failure result
- [ ] Follow existing use case patterns in the codebase
- [ ] Include proper transaction handling

## Task 5: Add DELETE endpoint to ProjectsController
**Agent:** BE

Add a DELETE HTTP endpoint to the existing ProjectsController to handle project deletion requests.

## Acceptance Criteria
- [ ] Add `Delete(int id)` method to ProjectsController
- [ ] Accept project ID as route parameter
- [ ] Invoke DeleteProjectUseCase
- [ ] Return 204 No Content on successful deletion
- [ ] Return 404 Not Found when project doesn't exist
- [ ] Return 409 Conflict when deletion fails due to constraints
- [ ] Follow existing controller patterns and error handling
- [ ] Include proper HTTP method routing [HttpDelete]

## Task 6: Add DELETE endpoint to FeaturesController
**Agent:** BE

Add a DELETE HTTP endpoint to the existing FeaturesController to handle feature deletion requests.

## Acceptance Criteria
- [ ] Add `Delete(int id)` method to FeaturesController
- [ ] Accept feature ID as route parameter
- [ ] Invoke DeleteFeatureUseCase
- [ ] Return 204 No Content on successful deletion
- [ ] Return 404 Not Found when feature doesn't exist
- [ ] Return 409 Conflict when deletion fails due to constraints
- [ ] Follow existing controller patterns and error handling
- [ ] Include proper HTTP method routing [HttpDelete]

## Task 7: Add DELETE endpoint to ArtifactsController
**Agent:** BE

Add a DELETE HTTP endpoint to the existing ArtifactsController to handle artifact deletion requests.

## Acceptance Criteria
- [ ] Add `Delete(int id)` method to ArtifactsController
- [ ] Accept artifact ID as route parameter
- [ ] Invoke DeleteArtifactUseCase
- [ ] Return 204 No Content on successful deletion
- [ ] Return 404 Not Found when artifact doesn't exist
- [ ] Return 409 Conflict when deletion fails due to constraints
- [ ] Follow existing controller patterns and error handling
- [ ] Include proper HTTP method routing [HttpDelete]

## Task 8: Create reusable ConfirmationModal component
**Agent:** FE

Build a reusable confirmation modal component that can be used for delete confirmations across the application.

## Acceptance Criteria
- [ ] Create `ConfirmationModal` component with TypeScript
- [ ] Accept props for title, message, confirmText, and cancelText
- [ ] Include onConfirm and onCancel callback functions
- [ ] Style with Tailwind CSS following existing design patterns
- [ ] Support keyboard navigation (Escape to cancel, Enter to confirm)
- [ ] Include focus management and accessibility attributes
- [ ] Handle loading state during confirmation action
- [ ] Support different confirmation button styles (danger, primary, etc.)

## Task 9: Add delete functionality to project list page
**Agent:** FE

Add delete buttons and confirmation flow to the projects list page.

## Acceptance Criteria
- [ ] Add delete button to each project row/card
- [ ] Integrate ConfirmationModal for delete confirmation
- [ ] Call DELETE /api/projects/{id} endpoint on confirmation
- [ ] Remove deleted project from local state immediately
- [ ] Show success message after successful deletion
- [ ] Display error message if deletion fails
- [ ] Handle loading state during deletion
- [ ] Follow existing UI patterns and styling

## Task 10: Add delete functionality to features page
**Agent:** FE

Add delete buttons and confirmation flow to the features list within a project.

## Acceptance Criteria
- [ ] Add delete button to each feature row/card
- [ ] Integrate ConfirmationModal for delete confirmation
- [ ] Call DELETE /api/features/{id} endpoint on confirmation
- [ ] Remove deleted feature from local state immediately
- [ ] Show success message after successful deletion
- [ ] Display error message if deletion fails
- [ ] Handle loading state during deletion
- [ ] Follow existing UI patterns and styling

## Task 11: Add delete functionality to artifacts page
**Agent:** FE

Add delete buttons and confirmation flow to the artifacts list within a feature.

## Acceptance Criteria
- [ ] Add delete button to each artifact row/card
- [ ] Integrate ConfirmationModal for delete confirmation
- [ ] Call DELETE /api/artifacts/{id} endpoint on confirmation
- [ ] Remove deleted artifact from local state immediately
- [ ] Show success message after successful deletion
- [ ] Display error message if deletion fails
- [ ] Handle loading state during deletion
- [ ] Follow existing UI patterns and styling
